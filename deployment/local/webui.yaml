apiVersion: apps/v1
kind: Deployment
metadata:
  name: character-catalog-webui-local
  namespace: character-catalog-local
  labels:
    app: character-catalog-webui-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: character-catalog-webui-local
  template:
    metadata:
      labels:
        app: character-catalog-webui-local
    spec:
      containers:
      - name: character-catalog-webui
        image: balkfet-webui:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: character-catalog-webui-local
  namespace: character-catalog-local
spec:
  selector:
    app: character-catalog-webui-local
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 80
  type: ClusterIP